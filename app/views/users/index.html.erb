<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
        <h2>User info</h2>
        <%= render 'info', user: current_user %>
        <h2 class="mt-3">New book</h2>
        <%= render 'books/form', book: @book %>
        <div class='col-md-8 offset-md-1'>
           <tr>
         <%= form_tag(method: "get") do %>
          <td><%= text_field_tag :keyword , params[:keyword],placeholder: ""%></td>
          <% end %>
       　　<%= form_tag(method: "get") do %>
          <td><%= select_tag "submit_name", options_for_select([["user", "user"],["book","book"]]) %></td>
        　<td><%= select_tag "submit_name", options_for_select([["完全一致", "完全一致"],["前方一致","前方一致"],["後方一致","後方一致"],["部分一致","部分一致"]]) %></td>
        　<td><%= submit_tag "検索" %></td>
        　</tr>
　<% end %>
　<%= @submits_searched %>
        <h2>Users</h2>
        <%= render 'index',users: @users %>
